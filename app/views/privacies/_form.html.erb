<%= form_for(@privacy) do |f| %>
  <% if @privacy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@privacy.errors.count, "error") %> prohibited this researcher from being saved:</h2>

      <ul>
      <% @privacy.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.select :namepriv, [['Private', 0], ['Collaborators Only', 1], ['Public', 2]] %>
  </div>
<div class="field">
    <%= f.label :phone_number %><br />
    <%= f.select :phonepriv, [['Private', 0], ['Collaborators Only', 1], ['Public', 2]] %>
  </div>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.select :emailpriv, [['Private', 0], ['Collaborators Only', 1], ['Public', 2]] %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
